{
  "name": "lts-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for LTS-2026 Loitering Tracking System — Claude & ChatGPT integration",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:http": "TRANSPORT=http node index.js",
    "dev": "node --watch index.js",
    "dev:http": "TRANSPORT=http node --watch index.js",
    "test": "node --test --experimental-test-isolation=none test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
